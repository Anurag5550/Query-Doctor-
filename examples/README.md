# Examples

This folder contains representative PostgreSQL plan examples for Query Doctor.

- `bad-plan.txt` — a suspicious query plan with heavy filtering and a large mismatch
- `healthy-plan.txt` — a healthy plan that should not trigger major findings
